{
    "filter_punctuation": true,
    "regex_filters": [
        "^rt$", "^…$", "^n?https?.+$", "^(u.+|n)$", "^[.]+$", "^[0-9]+$"
    ],
    "lower": true,
    "stopwords_language": "english",
    "normalize_unicode": true,
    "remove_break_lines": true,
    "hyperparameters": {
        "num_words": 10000,
        "hidden_layer_size": 0,
        "n_hidden_layers": 0,
        "dropout": 0.0,
        "batch_size": 16,
        "epochs": 10
    }
}